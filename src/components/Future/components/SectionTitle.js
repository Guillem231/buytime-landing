import React from 'react';
import styles from '../styles/Future.module.css';

const SectionTitle = () => {
  return (
    <div className={styles.title}>
      <h2>The Future of Time</h2>
      <p className={styles.subtitle}>A marketplace that transforms your time into value</p>
    </div>
  );
};

export default SectionTitle;
